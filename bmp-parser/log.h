#pragma once

#include "types.h"

class String;
class Base;

void log();
char log(char a, int newLine=1);
int log(int a, int newLine=1);
const char* log(const char* a, int newLine=1);
char* log(char* a, int newLine=1);